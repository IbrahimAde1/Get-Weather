name: weather_app
description: A new Flutter project.

publish_to: "none"
version: 2.0.0+2
environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.5
  flutter_bloc: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Sigmar
      fonts:
        - asset: fonts/Sigmar-Regular.ttf
